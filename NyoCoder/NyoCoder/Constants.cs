namespace NyoCoder
{
    public static class Constants
    {
        // Maximum content length for file reads and other operations (in characters)
        public const int MAX_CONTENT_LENGTH = 100000;
        
        // SearXNG instance URL (empty if not configured)
        public static string SEARXNG_INSTANCE = "";
    }
}
