<UserControl x:Class="NyoCoder.NyoCoder_VSIX.OptionsPageControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="400"
             Loaded="UserControl_Loaded"
             Focusable="True">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Title -->
        <Label Grid.Row="0" 
               Content="Options:" 
               FontWeight="Bold" 
               FontSize="12"
               Margin="0,0,0,16"/>
        
        <!-- API Key -->
        <Label Grid.Row="1" 
               Content="API Key:" 
               Margin="0,0,0,4"/>
        <PasswordBox Grid.Row="2" 
                     x:Name="txtApiKey"
                     PasswordChanged="txtApiKey_PasswordChanged"
                     Margin="0,0,0,16"
                     Height="23"
                     Focusable="True"
                     IsTabStop="True"/>
        
        <!-- LLM Server -->
        <Label Grid.Row="3" 
               Content="LLM Server (OpenAI Compatible):" 
               Margin="0,0,0,4"/>
        <TextBox Grid.Row="4" 
                 x:Name="txtLlmServer"
                 TextChanged="txtLlmServer_TextChanged"
                 Margin="0,0,0,16"
                 Height="23"
                 Focusable="True"
                 IsTabStop="True"/>
        
        <!-- Model -->
        <Label Grid.Row="5" 
               Content="Model:" 
               Margin="0,0,0,4"/>
        <TextBox Grid.Row="6" 
                 x:Name="txtModel"
                 TextChanged="txtModel_TextChanged"
                 Margin="0,0,0,16"
                 Height="23"
                 Focusable="True"
                 IsTabStop="True"/>
    </Grid>
</UserControl>
