<UserControl x:Class="NyoCoder.NyoCoderControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vsfx="clr-namespace:Microsoft.VisualStudio.Shell;assembly=Microsoft.VisualStudio.Shell.10.0"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"
             Name="NyoCoderWindow"
             Background="{DynamicResource {x:Static vsfx:VsBrushes.ToolWindowBackgroundKey}}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Status bar showing token usage -->
        <Border x:Name="TokenStatusBar"
                Grid.Row="0"
                Background="#2D2D30"
                BorderBrush="#3F3F46"
                BorderThickness="0,0,0,1"
                Padding="6,3">
            <TextBlock x:Name="TokenStatusText"
                       Foreground="#E0E0E0"
                       FontFamily="Segoe UI"
                       FontSize="11"
                       Text="Tokens: 0"/>
        </Border>
        
        <!-- Output text box -->
        <RichTextBox x:Name="OutputTextBox"
                     Grid.Row="1"
                     IsReadOnly="True"
                     VerticalScrollBarVisibility="Auto"
                     HorizontalScrollBarVisibility="Auto"
                     Foreground="{DynamicResource {x:Static vsfx:VsBrushes.ToolWindowTextKey}}"
                     Background="White"
                     FontFamily="Consolas"
                     FontSize="12"
                     Padding="4"
                     BorderThickness="0">
            <FlowDocument Background="White" LineHeight="14" LineStackingStrategy="BlockLineHeight">
                <FlowDocument.Resources>
                    <Style TargetType="{x:Type Paragraph}">
                        <Setter Property="Margin" Value="0"/>
                        <Setter Property="Padding" Value="0"/>
                    </Style>
                </FlowDocument.Resources>
                <Paragraph>
                    <Run Text="NyoCoder output will appear here..."/>
                </Paragraph>
            </FlowDocument>
        </RichTextBox>
        
        <!-- Button panel -->
        <StackPanel x:Name="ButtonPanel"
                    Grid.Row="2"
                    Orientation="Horizontal"
                    Height="36"
                    Margin="2"
                    Visibility="Collapsed"/>
        
        <!-- Input bar -->
        <Border x:Name="InputBar"
                Grid.Row="3"
                Background="{DynamicResource {x:Static vsfx:VsBrushes.ToolWindowBackgroundKey}}"
                BorderBrush="{DynamicResource {x:Static vsfx:VsBrushes.ToolWindowBorderKey}}"
                BorderThickness="0,1,0,0"
                Padding="4"
                Visibility="Collapsed">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox x:Name="InputBox"
                         Grid.Column="0"
                         Margin="0,0,4,0"
                         MinHeight="48"
                         Height="48"
                         AcceptsReturn="True"
                         TextWrapping="Wrap"
                         VerticalContentAlignment="Top"
                         VerticalScrollBarVisibility="Auto"
                         PreviewKeyDown="InputBox_PreviewKeyDown"/>
                <StackPanel Grid.Column="1"
                           Orientation="Vertical"
                           VerticalAlignment="Top">
                    <Button x:Name="InputSendButton"
                            Content="Send"
                            MinWidth="60"
                            MinHeight="20"
                            Padding="8,1"
                            Margin="0,0,0,4"
                            Click="InputSendButton_Click"/>
                    <ToggleButton x:Name="AttachImageButton"
                            Content="Attach Image"
                            MinWidth="60"
                            MinHeight="20"
                            Padding="8,1"
                            Checked="AttachImageButton_Checked"
                            Unchecked="AttachImageButton_Unchecked"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
